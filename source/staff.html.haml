%h1 Équipe

%script#project-template{type: 'text/x-handlebars-template'}
  %li
    %a{href: 'https://github.com/{{user}}/{{repo}}'}
      {{repo}}

%h2 Actuelle
%ul.staff.current
  - data.staff.current.each do |member|
    = partial :staff_member, locals: { member: member }

%h2 Anciennes
%ul.staff.legacy
  - data.staff.legacy.each do |member|
    = partial :staff_member, locals: { member: member }
